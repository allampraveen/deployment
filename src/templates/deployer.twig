{# @var craft \craft\web\twig\variables\CraftVariable #}
{#
/**
 * deployer plugin for Craft CMS 3.x
 *
 * deployer index.twig
 *
 * @author    Praveen
 * @copyright Copyright (c) 2018 Praveen
 * @link      https://github.com/allampraveen/
 * @package   Deployer
 * @since     1.0.0
 */
#}

{% extends "_layouts/cp" %}
{% import "_includes/forms" as forms %}

{% do view.registerAssetBundle("smsg\\deployer\\assetbundles\\deployer\\DeployerAsset") %}
{% do view.registerAssetBundle("smsg\\deployer\\assetbundles\\deployercpsection\\DeployerCPSectionAsset") %}

{# Link for the ? icon at the bottom of the page #}
{% set docsUrl = "https://github.com/https://github.com/allampraveen/deployment/deployer/blob/master/README.md" %}

{# The title of this CP section #}
{% set title = "deployer" %}

{# The URL to this plugin's base CP section #}
{% set pluginCpUrl = url('deployer') %}

{# Get a URL to an image in our AssetBundle #}
{% set iconUrl = view.getAssetManager().getPublishedUrl('@smsg/deployer/assetbundles/deployercpsection/dist', true) ~ '/img/Deployer-icon.svg' %}

{# Content that should appear in the page header#}
{% set extraPageHeaderHtml %}
    <div class="buttons">
        <a href="{{ pluginCpUrl }}" class="btn submit add icon">{{ "Click Me!"|t('deployer') }}</a>
    </div>
{% endset %}

{# The content of the CP Section#}
{% set content %}
    <h2>{{ "Deployer CP Section body goes here"|t('deployer') }}</h2>
    <img src="{{ iconUrl }}" height="64" width="64" />
    <p class="textline"></p>
{% endset %}
